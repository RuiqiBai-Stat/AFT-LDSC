# AFT-LDSC
AFT-LDSC is a computationally efficient extension of LD score regression (LDSC) specifically engineered for time-to-event phenotypes. It integrates the accelerated failure time (AFT) model with a censoring-aware summary statistic, which can enable the robust estimation of SNP-heritability and genetic correlation even under heavy censoring (>90%).
